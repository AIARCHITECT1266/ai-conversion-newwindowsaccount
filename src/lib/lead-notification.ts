// Stub: Lead-Benachrichtigungen (noch nicht implementiert)
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function notifyHighScoreLead(_data: {
  tenantName: string;
  score: number;
  qualification: string;
  lastMessage: string;
  conversationId: string;
}): void {
  // Platzhalter – wird spaeter mit E-Mail-Provider implementiert
}
